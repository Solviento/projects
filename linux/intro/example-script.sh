#!/bin/bash

# Why use hash, bang, bin, bash?
# Lets the machine know that this is a script we can execute

echo "Hello, this machine is named $HOSTNAME"

# Linux is full of $(names), any quick reference sheet
# will give you a full of list of identifying terms

echo root
