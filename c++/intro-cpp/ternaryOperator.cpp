#include <string>
#include <fstream>
#include <iostream>
#include <vector>

using namespace std;

int main(){
	
	/* Format of a ternary operator 

	variable = (condition) ? true : false 

	If condition is true (true/1), then execute true side of operator, vice versa
	with false */

	int largestNum = (5 > 2) ? 5 : 2; // Will assign 5 to variable if condition is true

}
